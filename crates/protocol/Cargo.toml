[package]
name = "pshred-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Protocol definitions for Constellation pshred packets"

[features]
default = []
std = []

[dependencies]
